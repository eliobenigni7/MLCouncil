// Placeholder stub — replaced by the full app shell in Task 16.
import { Outlet } from "react-router-dom";

export function SidebarLayout() {
  return (
    <div className="app-shell">
      <main className="main-content">
        <Outlet />
      </main>
    </div>
  );
}

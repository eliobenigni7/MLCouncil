streamlit>=1.33.0
plotly>=5.20.0
pandas>=2.0.0
polars>=1.0.0
yfinance>=1.0
feedparser>=6.0.11
numpy>=1.26.0
scipy>=1.11.0
lightgbm>=4.3.0
shap>=0.45.0
hmmlearn>=0.3.2
cvxpy>=1.5.0
pyyaml>=6.0
loguru>=0.7.0
# Monitoring & drift detection (Agent 09)
evidently>=0.4.0
mlflow>=2.10.0
dagster>=1.6.0
dagster-graphql>=1.6.0
dagster-webserver>=1.6.0

# Online learning / drift (T1.2 — pure Python ADWIN + DDM; no native deps)

# TFT alpha challenger (T2.1 — shadow training/inference; CPU tests in CI)
torch>=2.0.0

# FinMA/RAG sentiment challenger (T2.2 — optional; mock backends if absent)
# chromadb>=0.4.0
# llama-cpp-python>=0.2.0

# DCC-GARCH covariance challenger (T3.4 — optional; sample-cov fallback if absent)
arch>=6.0.0

# Differentiable portfolio (T3.3 — implicit QP gradients via cvxpylayers + torch)
# cvxpylayers>=0.1.6  # optional spike dependency; keep out of core image until promotion gate passes

# Observability (T1.4 — optional at runtime; no-op when MLCOUNCIL_OTEL_ENABLED unset)
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0

# Admin API
-r requirements_api.txt
